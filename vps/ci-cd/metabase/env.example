# Metabase on Coolify Environment Configuration
# Copy this file to .env and update the values

# PostgreSQL Database Configuration
POSTGRES_DATABASE=metabase
POSTGRES_USER=metabase
POSTGRES_PASSWORD=<STRONG_PASSWORD_HERE>

# Additional Notes:
# - Change POSTGRES_PASSWORD to a strong password
# - The database will be created automatically on first run
# - Data persists in Docker volumes: metabase-data and postgres-data
# - Metabase will be accessible through Coolify proxy on port 3000
# - No external PostgreSQL port needed (internal communication only)