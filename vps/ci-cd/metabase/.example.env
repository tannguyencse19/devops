# Metabase on Coolify Environment Configuration
# Copy this file to .env and update the values

# PostgreSQL Database Configuration
POSTGRES_DATABASE=metabase
POSTGRES_USER=metabase
POSTGRES_PASSWORD=<STRONG_PASSWORD_HERE>

# Port Configuration (defined in docker-compose.yml)
# - Metabase Web UI: http://localhost:5700 (maps to internal 3000)
# - PostgreSQL External Access: localhost:5710 (maps to internal 5432)

# Additional Notes:
# - Change POSTGRES_PASSWORD to a strong password
# - The database will be created automatically on first run
# - Data persists in Docker volumes: metabase-data and postgres-data
# - Metabase will be accessible through Coolify proxy on port 5700
# - PostgreSQL available for external connections on port 5710