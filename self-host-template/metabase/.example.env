# Metabase Self-Host Configuration
# Copy this file to .env and update the values

# PostgreSQL Database Configuration
POSTGRES_DATABASE=metabase
POSTGRES_USER=metabase
POSTGRES_PASSWORD=<STRONG_PASSWORD_HERE>

# Metabase Configuration
# The database connection is automatically configured via MB_DB_* environment variables
# in docker-compose.yml

# Port Configuration (defined in docker-compose.yml)
# - Metabase Web UI: http://localhost:5700
# - PostgreSQL External Access: localhost:5710

# Additional Notes:
# - Change POSTGRES_PASSWORD to a strong password
# - The database will be created automatically on first run
# - Data persists in Docker volumes: metabase-data and postgres-data