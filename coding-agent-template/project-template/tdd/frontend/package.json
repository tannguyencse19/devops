{
    "name": "frontend",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
      "dev": "vite --host",
      "build": "tsc -b && vite build",
      "lint": "eslint . && tsc -b",
      "preview": "vite preview",
      "test:whole-app": "vitest run"
    },
    "dependencies": {
    },
    "devDependencies": {
    }
  }